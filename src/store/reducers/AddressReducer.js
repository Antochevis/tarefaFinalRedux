const INITIAL_STATE = {

}

function AddressReducer(state = INITIAL_STATE, action) {
  if (action.type === '') {
    return {
      ...state
    }
  }

  if (action.type === '') {
    return {
      ...state
    }
  }
  return state
}

export default AddressReducer